#Workflow Name
name: First Workflow
#Trigger
on: 
  push:
  workflow_dispatch: 
  
jobs:
  Hello-World:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World
        run: echo "Hello World"
  #Second Job
  echo-goodbye:
    runs-on: ubuntu-slim
    steps:
      - name: say Goodbye
        run: echo "Goodbye!!"
      #multiple commands
      - name: Multi-line bash
        run: |
          echo "I am"
          echo "Multi Line"
          echo "Script"
  
  


